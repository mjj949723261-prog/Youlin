server:
  port: 8080

spring:
  application:
    name: youlin-server

  # H2 内存数据库配置 (零配置开箱即用)
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:youlindb;DB_CLOSE_DELAY=-1;MODE=MYSQL
    username: sa
    password: 

  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# 微信小程序配置
wx:
  miniapp:
    appid: wxc3781b268c2b5fb8
    secret: 17c35f7527d0b9a27fa341abce811a16
